<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
 <mapper namespace="Member">

	<select id="findKakao" parameterType="java.util.HashMap" resultType="sol.one.VO.KakaoDTO">
		select * from kakao_table where k_name=#{nickname} and k_email=#{email}
	</select>
	
	<insert id="kakaoInsert" parameterType="java.util.HashMap">
		insert into kakao_table(k_name, k_email)
			values(#{nickname}, #{email})
	</insert>
	
	<select id="kakaoNumber" parameterType="sol.one.VO.KakaoDTO" resultType="sol.one.VO.KakaoDTO">
		select * from kakao_table where k_name=#{k_name} and k_email=#{k_email}
	</select>
	
</mapper>