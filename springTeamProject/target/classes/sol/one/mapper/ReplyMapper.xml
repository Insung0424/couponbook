<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="sol.one.mapper.ReplyMapper">

	<insert id="insert_co">
		insert into pj_comment(product_id,user_id,comment_content,comment_write_time,comment_no,comment_no_level,group_no)
		values (#{product_id},#{user_id},#{comment_content},current_date,seq_com_no.nextval,null,seq_com_no.currval)
	</insert>
	
	<insert id="insert_co_tab">
		insert into pj_comment(product_id,user_id,comment_content,comment_write_time,comment_no,comment_no_level,group_no)
		values (#{product_id},#{user_id},#{comment_content},current_date,seq_com_no.nextval,1,#{group_no})
	</insert>
	
	<update id="tabGroupNoUp">
		update pj_comment set group_no = group_no + 1 
		where product_id = #{product_id} and user_id = #{user_id} and comment_no_level = #{comment_no_level}
	</update>
	
	<select id="read" resultType="sol.one.VO.replyVO">
		select * from pj_comment 
		order by if(isnull(comment_no_level),comment_no,group_no)
		where product_id = #{product_id} and user_id = #{user_id_1} or user_id = #{user_id_2}
	</select>
	
	<select id="readAll" resultType="sol.one.VO.replyVO">
		select * from 
		(select * from pj_comment order by comment_write_time desc)
		where product_id = #{product_id} order by group_no desc
	</select>
	
	<delete id="delete">
		delete from pj_comment where comment_no = #{comment_no}
	</delete>
	
	<delete id="deleteAll">
		delete from pj_comment where product_id = #{product_id}
	</delete>
	
	<update id="update" parameterType="map">
		update pj_comment
		set comment_content = #{comment_content}
		where comment_no = #{comment_no}
	</update>
</mapper>