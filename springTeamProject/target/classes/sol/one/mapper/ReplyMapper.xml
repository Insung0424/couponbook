<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="sol.one.mapper.ReplyMapper">

	<insert id="insert_co">
		insert into pj_comment(product_id,user_id,comment_content,comment_write_time)
		values (#{product_id},#{user_id},#{comment_content},sysdate)
	</insert>
	
	<select id="read" resultType="sol.one.VO.replyVO">
		select * from pj_comment where user_id = #{user_id}
	</select>
	
	<delete id="delete">
		delete from pj_comment where product_id = #{product_id} and user_id = #{user_id} and comment_content = #{comment_content}
	</delete>
	
	<update id="update" parameterType="map">
		update pj_comment
		set comment_content = #{change_comment}
		where product_id = #{vo.product_id} and user_id = #{vo.user_id} and comment_content = #{vo.comment_content}
	</update>
</mapper>